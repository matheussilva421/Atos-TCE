# Handoff — Tarefa 0.8 (`-Apply`) — 2026-09-10

## Resumo

O `-Apply` corrigido foi executado com Windows PowerShell 5.1 e moveu os 16.074 itens aprovados, totalizando 23.126.367.618 bytes. O recibo passou com `not_moved=0`. A reanálise r3 passou com exit code 0. A verificação pós-quarentena falhou no início porque o default de `verify-post-quarantine.ps1` contém o caminho mojibake `ComplementaÃ§Ã£o de Atos`; o passo 7 não foi executado. Nenhum purge foi executado.

## Evidências

- Estado inicial: `HEAD == origin/main == d297b6644a19033d30a8e463585067f42e408a79`.
- Manifesto r2: SHA-256 `A7994FF698DFABC690A0A50654D0C3A68996677DE4E336FF0AF6075DA11E492F`, 16.074 itens, 23.126.367.618 bytes.
- Probe: exit `0`; `owned=0`, `exclusive_ok=794`, `locked=0`, `absent=0`.
- WhatIf: exit `0`; `approved_items=16074`, `approved_bytes=23126367618`.
- Apply corrigido: exit `0`; `approved_items=16074`, `approved_bytes=23126367618`.
- Recibo `tmp\quarantine\20260911-012750-653\receipt.json`: exit `0`; `items=16074`, `moved=16074`, `not_moved=0`, `moved_bytes=23126367618`.
- Reanálise r3: exit `0`; `entries=31721`, `skipped_directories=4`, `warnings=4`.
- Verificador padrão: exit `1`; `Set-Location` falhou para `C:\Users\slvma\Downloads\Github\ComplementaÃ§Ã£o de Atos`.
- Testes focais: não executados devido ao gate 6 falho.

## Arquivos criados/alterados nesta sessão

- `.superpowers/sdd/2026-09-10-plano-consolidacao-main-e-conclusao/task-0.8-apply-report.md` — relatório atualizado.
- `docs/notes/2026-09-10-task-0.8-apply-handoff.md` — este handoff atualizado.
- `tmp/fase0/apply-run.log` — saída do apply via `Tee-Object`.
- `tmp/fase0/workspace-manifest-r3.json` — manifesto produzido pela reanálise.
- `tmp/quarantine/20260911-012750-653/` — quarentena e recibo produzidos pelo `-Apply` autorizado.

## Pendências

1. Resolvida pelo controlador: o script estava em UTF-8 sem BOM e o PowerShell
   5.1 lia o literal acentuado como Windows-1252; os mesmos bytes foram
   reescritos como UTF-8 com BOM (conteúdo idêntico).
2. Reexecutada com o caminho correto: `preserved_checked=13485`,
   `preserved_missing=0`, `approved_still_present=0`, `not_moved=0`; restou
   apenas `preserved_hash_changed=58`, auditado item a item (38 `.pyc`, 19
   rastreados limpos frente ao Git, 1 log ignorado; mtime máxima 21:27,
   anterior ao `-Apply`), sem efeito da quarentena.
3. Executada: `Test-WorkspaceCleanup.ps1` 307/307, exit 0, duas execuções.
4. Mantida: `-PurgeQuarantine`, `-Resume` de purge e exclusão nunca executados.

## GitHub

Nenhum `git add`, `commit` ou `push` foi executado pelo worker. O controlador
versiona este handoff, o `task-0.8-apply-report.md` e os documentos canônicos no
commit de fechamento da Tarefa 0.9; a quarentena e os manifestos/logs de `tmp/`
permanecem locais (não rastreados).
